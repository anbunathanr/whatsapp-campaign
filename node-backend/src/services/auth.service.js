/**
 * Authentication Service
 * Handles user registration, login, token management, and account lockout.
 * Full implementation: Tasks 2.2, 2.3, 2.4
 */

// Stub — will be implemented in Tasks 2.2 and 2.3
const register = async (_userData) => {
  throw new Error('Not implemented yet');
};

const login = async (_email, _password) => {
  throw new Error('Not implemented yet');
};

const refreshToken = async (_refreshToken) => {
  throw new Error('Not implemented yet');
};

const changePassword = async (_userId, _currentPassword, _newPassword) => {
  throw new Error('Not implemented yet');
};

module.exports = { register, login, refreshToken, changePassword };
